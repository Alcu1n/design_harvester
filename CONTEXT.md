# Design Harvester

从公共网页提取设计语言，保存为个人长期使用的设计资料。

## Language

**设计条目（Design）**：同一规范化页面 URL 的长期收藏，包含用户名称、标签和备注。
_Avoid_: 网站快照、文档版本

**采集快照（Snapshot）**：一次访问所观察到的来源、三个视口截图和浏览器证据。
_Avoid_: 生成版本

**生成版本（Version）**：基于指定采集快照产生的一组设计分析、设计文档和质量结论。
_Avoid_: 重新采集

**执行任务（Run）**：一次采集或重新生成的执行过程，可以暂停、恢复或取消。
_Avoid_: 设计条目

**浏览器证据（Evidence）**：浏览器实际观察的元素、计算样式、几何和页面状态。观察不足不等于数值为零。

**设计解释（Interpretation）**：根据证据推断的设计意图、特征和使用规则。

**原生适配（Native adaptation）**：将设计意图转化为 Apple 平台行为的建议，不是原网页的观察事实。

**合格版本（Qualified version）**：文档完整、格式有效、质量分达到发布门槛且没有严重质检问题的生成版本。

**默认版本（Default version）**：资料库默认展示和导出的最新合格版本。尚无合格版本时显示已有结果及其限制。
