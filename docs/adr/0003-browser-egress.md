# 浏览器独立运行，出口代理校验实际目标地址

原文三容器建议扩展为 web、worker、postgres、browser、egress 和一次性迁移服务。浏览器只接入内部采集网络，没有数据库、授权卷或资产库挂载；对外连接经出口代理，由代理解析并连接已校验的公共 IP，避免仅在应用 DNS 预检后由浏览器再次解析。WebSocket 在采集层关闭，QUIC 和非代理 WebRTC 禁用。内部端口不发布到宿主，UI 默认绑定回环地址；远程访问由可信网关或私网负责。

本地 ALLOW_LOCAL_BROWSER 仅供显式开发烟测，不作为容器网络隔离的验收证据。
